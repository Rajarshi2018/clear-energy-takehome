{
  "name": "clear-energy-takehome",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mock-api": "json-server mock-api.json --port 4000",
    "test": "vitest run",
    "test:watch": "vitest",
    "customer": "npm run start --workspace=customer",
    "driver": "npm run start --workspace=driver",
    "admin": "npm run start --workspace=admin-mobile"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
